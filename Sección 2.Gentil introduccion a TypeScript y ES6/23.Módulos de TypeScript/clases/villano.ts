export {}; // Esto se utiliza para que no indique como variables duplicadas
// Sección 2. Gentil introducción a TypeScript y ES6
// 23. Módulos de TypeScript

// Se deja todo comentado ya que en este momento no se está ejecutando SystemJS
/*
export class Villano {
	nombre:string;
	plan:string;

	constructor(nombre:string, plan:string) {
		this.nombre = nombre;
		this.plan = plan;
	}

	imprimirPlan() {
		console.log(`El plan es: ${this.plan}`);
	};
}
*/
